services:
  plate-api:
    build: .
    container_name: license-plate-api
    ports:
      - "8001:8000"
    volumes:
      - ./data:/data
      - ./paddle_cache:/tmp/paddleocr
    environment:
      - PYTHONUNBUFFERED=1
      - PADDLEOCR_HOME=/tmp/paddleocr
    restart: unless-stopped
    networks:
      - plate-network

networks:
  plate-network:
    name: plate-network
    driver: bridge
